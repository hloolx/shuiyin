# 🚀 最终部署命令

## ✅ 准备工作已完成
- ✅ README.md logo 已更新为 `public/images/logo108.png`
- ✅ 所有 GitHub 链接已修正
- ✅ 添加了腾讯云赞助信息
- ✅ 本地只有 master 分支

---

## 📦 完整部署流程

在 PowerShell 中依次执行以下命令：

### 步骤 1: 提交最新改动

```powershell
cd "c:\Users\12153\daima\添加水印\react-watermark"
git add .
git commit -m "完善项目文档和页面信息

- 更新 README.md 头部 logo
- 添加腾讯云 EdgeOne 赞助信息
- 更新作者信息和博客链接
- 修正所有 GitHub 地址为正确链接
- 页面 Footer 添加博客入口"
```

### 步骤 2: 推送到 master 分支

```powershell
git push -u origin master --force
```

### 步骤 3: 删除远程的 main 分支

```powershell
git push origin --delete main
```

如果远程没有 main 分支，会显示错误信息，可以忽略。

---

## 🔧 一键执行（推荐）

直接复制这个完整命令到 PowerShell 执行：

```powershell
cd "c:\Users\12153\daima\添加水印\react-watermark"; git add .; git commit -m "完善项目文档和页面信息"; git push -u origin master --force; git push origin --delete main
```

---

## 📊 验证结果

### 1. 检查本地分支
```powershell
git branch
```
应该只显示 `* master`

### 2. 检查远程分支
```powershell
git branch -r
```
应该只显示 `origin/master`

### 3. 访问 GitHub
访问 https://github.com/hloolx/shuiyin 检查：
- ✅ 只有 master 分支
- ✅ README.md 显示正常（logo、徽章、赞助信息）
- ✅ 文件完整（无 node_modules 和 .claude）

---

## 💡 常见问题

### Q1: 如果提示 "everything up-to-date"
说明没有新的改动，可以跳过 commit 直接执行：
```powershell
git push -u origin master --force
git push origin --delete main
```

### Q2: 如果删除 main 分支失败
可能的原因：
1. 远程仓库没有 main 分支（正常，可以忽略）
2. main 是默认分支（需要先在 GitHub 设置中改为 master）

**如何修改默认分支：**
1. 访问 https://github.com/hloolx/shuiyin/settings/branches
2. 在 "Default branch" 部分点击切换图标
3. 选择 `master` 作为默认分支
4. 确认后再执行删除命令

### Q3: 如果推送需要认证
```powershell
# 使用 HTTPS 方式会提示输入 GitHub 用户名和密码（或 token）
# 或者配置 SSH 方式：
git remote set-url origin git@github.com:hloolx/shuiyin.git
```

---

## ✨ 最终效果

部署成功后，GitHub 仓库将显示：
- 🎨 精美的 README 头部（logo + 徽章）
- 💰 腾讯云 EdgeOne 赞助信息
- 👨‍💻 作者信息（GitHub + 博客）
- 🙏 致谢原项目 joyqi/sfz
- 📄 GPL-3.0 开源协议
- 🌿 只有 master 分支

---

准备好后，直接执行上面的命令即可！🎉
